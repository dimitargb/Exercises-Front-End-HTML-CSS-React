The idea here is whenever the order is not a priority order to display a Button below the order, 
where the user can click to make the order priority order.

So we make a new Component called UdateOrder in the Order.jsx Element