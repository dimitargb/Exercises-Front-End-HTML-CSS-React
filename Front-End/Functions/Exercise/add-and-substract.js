function addAndSubstract(a, b, c){

    function sumNumbers(a, b){
        let result = a + b;
        return result;
    }

    function substarct( result, c){
        let finalResult = result - c;
        return finalResult;
    }
      
    console.log(substarct(sumNumbers(a, b), c));
}
addAndSubstract(1,
    17,
    30)